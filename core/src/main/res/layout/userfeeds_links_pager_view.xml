<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="50dp"
    android:background="@android:color/black">

    <android.support.v4.view.ViewPager
        android:id="@+id/userfeeds_links_pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <ProgressBar
        android:id="@+id/userfeeds_links_loader"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"/>

    <TextView
        android:id="@+id/userfeeds_links_empty_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/userfeeds_link_background"
        android:gravity="center"
        android:textColor="@color/userfeeds_link_text_color"
        android:visibility="gone"/>

    <LinearLayout
        android:id="@+id/userfeeds_links_details_panel"
        android:layout_width="50dp"
        android:layout_height="match_parent"
        android:layout_gravity="right"
        android:orientation="vertical"
        android:visibility="gone">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="@android:color/white"
            android:drawableLeft="@drawable/ethereum"
            android:gravity="center"
            android:text="ETH"
            android:textStyle="bold"/>

        <TextView
            android:id="@+id/userfeeds_link_probability"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="#c2f9c2"
            android:gravity="center"
            tools:text="P 20%"/>

    </LinearLayout>

</FrameLayout>
